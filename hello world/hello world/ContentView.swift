import SwiftUI

struct ContentView: View {

    var body: some View {
        Text("hello_worldApp")
            .frame(maxWidth: .infinity, maxHeight: .infinity)
            .background(Color.clear) 
    }
}

#Preview {
    ContentView()
}
